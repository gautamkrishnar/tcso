/**
 * Run the following commands to install the necessary module
 * npm install opn
 */
var opn = require('opn');

try {
    //TODO TASK
}
catch (error) {
    opn("http://stackoverflow.com/search?q=[ts]+" + exception.message); //Opens the page on your default browser
}